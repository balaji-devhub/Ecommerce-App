import { MobileFilterBar } from './Filter'

const TopFilterBar = () => {
  return (
    <MobileFilterBar>
      <button>Category</button>
      <button>Price</button>
      <button>Rating</button>
    </MobileFilterBar>
  )
}

export default TopFilterBar
